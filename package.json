{
  "name": "billiards-champion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "vite build --configLoader runner && node scripts/build-worker.mjs",
    "deploy": "npm run build && wrangler deploy --config wrangler.jsonc",
    "preview": "vite preview",
    "test": "node --test tests/game-rules.test.mjs"
  },
  "dependencies": {
    "matter-js": "0.20.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.1.5",
    "wrangler": "4.110.0"
  }
}
